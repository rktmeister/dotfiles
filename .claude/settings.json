{
  "env": {
    "DISABLE_ZOXIDE": "1",
    "CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC": "1",
    "ENABLE_BACKGROUND_TASKS": "1",
    "BASH_DEFAULT_TIMEOUT_MS": "600000",
    "EDITOR": "nvim",
    "DISABLE_AUTOUPDATER": "1"
  },
  "includeCoAuthoredBy": false,
  "permissions": {
    "allow": [
      "mcp__ask-llm__ask_gemini",
      "mcp__ask-llm__ask_deepseek",
      "mcp__ask-llm__ask_openai",
      "mcp__ask-llm__create_session",
      "mcp__ask-llm__list_sessions",
      "mcp__ask-llm__save_sessions",
      "mcp__ask-llm__rename_session",
      "mcp__ask-llm__get_session_messages",
      "mcp__ask-llm__delete_session",
      "mcp__ask-llm__clear_session"
    ],
    "deny": [
      "NotebookRead",
      "NotebookEdit"
    ]
  },
  "model": "opus",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash|Run",
        "hooks": [
          {
            "type": "command",
            "command": "python3 ~/.claude/hooks/pre_tool_use_uv.py",
            "timeout": 10
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Bash|Run",
        "hooks": [
          {
            "type": "command",
            "command": "python3 ~/.claude/hooks/post_tool_use_uv.py"
          }
        ]
      }
    ],
    "Notification": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "python3 ~/.claude/hooks/notification_uv.py"
          }
        ]
      }
    ]
  },
  "statusLine": {
    "type": "command",
    "command": "input=$(cat); cwd=$(echo \"$input\" | jq -r '.workspace.current_dir'); model=$(echo \"$input\" | jq -r '.model.display_name'); output=\"\\033[34m${cwd}\\033[0m\"; if git -C \"$cwd\" rev-parse --git-dir >/dev/null 2>&1; then branch=$(git -C \"$cwd\" --no-optional-locks rev-parse --abbrev-ref HEAD 2>/dev/null); changes=\"\"; git -C \"$cwd\" --no-optional-locks diff --quiet 2>/dev/null && git -C \"$cwd\" --no-optional-locks diff --cached --quiet 2>/dev/null || changes=\"*\"; ahead_behind=\"\"; upstream=$(git -C \"$cwd\" --no-optional-locks rev-parse --abbrev-ref @{u} 2>/dev/null); if [ -n \"$upstream\" ]; then ahead=$(git -C \"$cwd\" --no-optional-locks rev-list --count @{u}..HEAD 2>/dev/null || echo 0); behind=$(git -C \"$cwd\" --no-optional-locks rev-list --count HEAD..@{u} 2>/dev/null || echo 0); [ \"$behind\" -gt 0 ] && ahead_behind=\"${ahead_behind}⇣\"; [ \"$ahead\" -gt 0 ] && ahead_behind=\"${ahead_behind}⇡\"; fi; output=\"${output} \\033[90m${branch}${changes}\\033[0m\"; [ -n \"$ahead_behind\" ] && output=\"${output} \\033[36m${ahead_behind}\\033[0m\"; fi; output=\"${output} \\033[90m[${model}]\\033[0m\"; printf \"%b\" \"$output\""
  },
  "alwaysThinkingEnabled": false
}
